# MONARCH PASSPORT RECOVERY LOG

## Session Handoff: May 9, 2026
**Origin**: `monarch-labs` (Storefront)

### Context & Findings
1. **The Issue**: User is unable to create/login to an account using email. The Privy modal shows "Something went wrong".
2. **Investigation Progress**:
   - In `monarch-labs`, we implemented real Privy authentication on the `/passport` (Profile) route.
   - We discovered that the storefront was previously redirecting to the external `passport.papillonbrand.us` instead of the local Passport app.
   - We updated the storefront to use internal routes and point to a local `/claim` protocol.
3. **Hypothesis for this Project**:
   - The standalone `monarch-passport` app likely has the "true" login logic.
   - The "Something went wrong" error usually points to:
     - `VITE_PRIVY_APP_ID` being missing or incorrect in `.env`.
     - The current URL (`localhost:8080` or `localhost:5173`) not being added to **Allowed Origins** in the Privy Dashboard.
     - A mismatch between the App ID and the project configuration.

### Immediate Action Items
- [ ] Verify `.env` file for `VITE_PRIVY_APP_ID`.
- [ ] Check `src/providers/PrivyProvider.tsx` (or equivalent) for config.
- [ ] Test the login flow and inspect Network/Console logs for the specific Privy error code.
- [ ] Ensure that once logged in, the Passport correctly hydates from the Solana blockchain (as we saw in the `monarch-labs` hooks).

---
*Generated by Gemini CLI during storefront session.*
